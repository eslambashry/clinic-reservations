# payments

**MVP** (pay-at-clinic; online payment is fast-follow) — owns `PaymentIntent`, `PaymentAttempt`, `PaymentSplit`, `Refund`, `ProviderLedgerEntry` (see `prisma/schema/payments.prisma`), per File 11 Part 03.
