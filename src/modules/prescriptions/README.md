# prescriptions

**MVP** (patient-uploaded only; doctor-issued is POSTPONE) — owns `DrugCatalog`, `Prescription`, `PrescriptionItem`, `PrescriptionImage`, `PrescriptionReview` (see `prisma/schema/prescriptions.prisma`), per File 11 Part 03.
